#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - checks the length of a string
 * @s: The string to be evaluated
 * Return: The length of the string
*/

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}

